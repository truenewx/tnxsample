logging:
  config: classpath:log/logback-local.xml
tnxjee:
  profile:
    formal: false
server:
  host: localhost
spring:
  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: validate
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://${server.host}:3306/sample_core?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
      username: data
      password: datapwd