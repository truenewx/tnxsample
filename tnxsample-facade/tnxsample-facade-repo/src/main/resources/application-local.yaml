spring:
  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://${server.host}:3306/example_core?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
    username: data
    password: datapwd